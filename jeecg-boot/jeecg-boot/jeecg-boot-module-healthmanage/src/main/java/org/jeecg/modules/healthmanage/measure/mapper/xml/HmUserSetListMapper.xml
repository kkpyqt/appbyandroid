<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.healthmanage.measure.mapper.HmUserSetListMapper">

    <select id="queryBySetImei"    resultType="org.jeecg.modules.healthmanage.measure.entity.HmUserSetList">

       select * from hm_user_set_list   where  upper(set_IMEI) = upper(#{set_imei,jdbcType=VARCHAR})
 order by create_time desc limit 1
    </select>

    <select id="queryBySetMacAdd"    resultType="org.jeecg.modules.healthmanage.measure.entity.HmUserSetList">

       select * from hm_user_set_list   where upper(set_macadd) = upper(#{set_macadd,jdbcType=VARCHAR})
 order by create_time desc limit 1
    </select>

    <select id="queryBySetId"    resultType="org.jeecg.modules.healthmanage.measure.entity.HmUserSetList">

       select * from hm_user_set_list   where  upper(set_id) = upper(#{set_id,jdbcType=VARCHAR})
       and user_id = #{user_id,jdbcType=VARCHAR}
 order by create_time desc limit 1
    </select>

</mapper>